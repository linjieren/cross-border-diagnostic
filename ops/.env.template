# 对外端口
NGINX_PORT=8080
DEV_PORT=8081
MAIN_PORT=80

# 后端
BACKEND_PORT=3000
NODE_ENV=production

# 反馈按钮 (dev=true, main=false)
FEATURE_FEEDBACK_BUTTON=false

# 数据库
DB_USER=app
DB_PASSWORD=app
DB_NAME=app
DEV_DB_PORT=5433

# Redis (内部)

# 其他
TZ=Asia/Shanghai
